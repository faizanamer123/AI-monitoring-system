[pytest]
testpaths = tests
timeout = 900
addopts = -ra --strict-markers
markers =
    e2e: full-pipeline end-to-end tests (slow: trains a real model)
    camera: requires physical camera access
    accuracy: measures stage accuracy against ground truth
